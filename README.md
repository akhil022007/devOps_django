This project automates the build and deployment process of a Django application using Jenkins and Docker.

You have:

A Dockerfile that builds and runs the Django application

A Jenkinsfile or Jenkins configuration to automate:

Pulling code

Building Docker image

Running the Django container

Optional: migrations / tests

This project serves as a CI/CD automation setup for Django.
